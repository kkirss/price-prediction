{
  "name": "@price-prediction/api-database",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "files": [
    "prisma",
    "src"
  ],
  "main": "src/index.ts",
  "module": "src/index.ts",
  "types": "src/index.ts",
  "scripts": {
    "dev": "prisma generate --watch",
    "build": "prisma generate",
    "lint": "ts-standard",
    "lint:fix": "ts-standard --fix",
    "lint-schema": "prisma format",
    "check-types": "tsc",
    "validate-schema": "prisma validate",
    "migrate-db-dev": "prisma migrate dev --skip-generate"
  },
  "dependencies": {
    "@prisma/client": "^5.3.1"
  },
  "peerDependencies": {
    "prisma": "^5.3.1"
  },
  "devDependencies": {
    "@price-prediction/tsconfig": "workspace:*",
    "prisma": "^5.3.1",
    "ts-standard": "^12.0.2",
    "typescript": "^5.2.2",
    "wait-on": "^7.0.1"
  },
  "ts-standard": {
    "ignore": [
      "dist-types"
    ]
  }
}
